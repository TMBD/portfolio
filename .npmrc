# Use a flat, npm-style node_modules layout (like npm/yarn) instead of pnpm's
# default symlinked store. This maximizes compatibility with Astro, astro-icon,
# Tailwind v4 and sharp, and avoids "cannot find module" edge cases on CI.
node-linker=hoisted
