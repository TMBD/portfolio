# Since pnpm 11 this file is read for registry/auth settings only; every other
# pnpm setting lives in pnpm-workspace.yaml.

# Pin the public registry so this project never depends on a corporate mirror
# configured at user level.
registry=https://registry.npmjs.org/
